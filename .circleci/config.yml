version: 2.1

jobs:
  build-job:
    docker:
      - image: ubuntu:22.04

    steps:
      - checkout
      - run:
          name: Install Node
          command: |
            apt update
            apt install -y nodejs
            node -v

workflows:
  build:
    jobs:
      - build-job